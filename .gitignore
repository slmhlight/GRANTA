# dependencies
node_modules/

# build output
dist/
dist-ssr/

# logs
.manus-logs/
*.log

# editor / OS
.DS_Store
*.local
*.swp

# local-only Claude settings
.claude/settings.local.json

# project recovery scratch (not part of the codebase)
_inspect/
_src_zip/

# generated preview (the live dataset is client/public/materials.json)
data/materials.preview.json

# R91 — generated by `pnpm build:data`. 매 commit 마다 6 MB+ diff 가 생겨 repo 비대화 + push 부담.
#       CI/Deploy workflow 는 install 후 `pnpm build:data` 단계로 직접 생성하므로 git tracking 불필요.
client/public/materials.json
client/public/build-meta.json
data/validation-report.md

# R100 — Hyundai Steel catalog OCR 산출 텍스트 (각 ~500 KB, source = PDF).
#         의사 결정 근거 자료로 보관하지만 repo tracking 은 불필요.
data/hyundai-steel-pt1-ocr.txt
data/hyundai-steel-pt2-ocr.txt
